import React from 'react';
import { Content } from '../newtab/Content';

export const ContentScriptForTest = () => {
    return (
        <React.StrictMode>
            <Content />
        </React.StrictMode>
    );
};
